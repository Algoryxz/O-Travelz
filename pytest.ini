[pytest]
testpaths = backend/tests
pythonpath = . backend
markers =
    integration: mark test as requiring a live PostgreSQL/PostGIS database instance
addopts = -m "not integration" --basetemp=.pytest_tmp -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning:pydantic.*:
